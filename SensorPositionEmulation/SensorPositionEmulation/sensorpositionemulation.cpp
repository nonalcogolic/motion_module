#include "sensorpositionemulation.h"

SensorPositionEmulation::SensorPositionEmulation(QWidget *parent)
    : QWidget(parent)
{
    ui.setupUi(this);
}

SensorPositionEmulation::~SensorPositionEmulation()
{

}
